# Web IA/UX Loop 2 — Implementation Notes

Date: 2026-05-27
Baseline HEAD: `97d57fb` → TBD
Task type: `ui_ux_polish`
Active workstream: Web IA/UX Loop 2

## Summary

修复 post-dogfood global red team audit (`docs/audits/2026-05-27-118-post-governance-global-red-team-audit.md`) 中暴露的 3 个 Web IA/UX P1 问题。本轮只修用户可见的 P1/P2，不做视觉 polish、新功能或架构重构。

## Changes

### 1. Sidebar: Dogfood 从 tools 移至 lab (`web/src/components/Sidebar.tsx`)

- `/dogfood` 从 "工具与诊断" 分组移除
- 加入 labItems（与 Graph、Sensemaking 同级），默认折叠
- 理由：Dogfood 是内部验证/工程证据，不是普通用户功能

### 2. DogfoodPage: 添加 LAB/INTERNAL 横幅 (`web/src/pages/DogfoodPage.tsx`)

- 页面顶部新增 amber 警告横幅
- 中文文案明确标注 "LAB / INTERNAL — 内部开发验证"
- 说明本页面数据仅在本地聚合，不调用外部服务或 LLM

### 3. LocalGraphPreview: 硬编码英文字符串替换为 i18n (`web/src/components/LocalGraphPreview.tsx`)

- "Local Graph Preview" → `t("local_graph.title")`
- "Local graph" → `t("local_graph.section_title")`
- 空状态英文段落 → `t("local_graph.empty")`
- related cards 计数 → `t("local_graph.related_count")`

### 4. i18n 新增键 (`web/src/lib/i18n.ts`)

zh:
- `local_graph.title`: "局部关系预览"
- `local_graph.related_count`: "{count} 张相关卡片"
- `local_graph.empty`: "暂无确定性关系..."
- `local_graph.section_title`: "局部关系图"

en:
- `local_graph.title`: "Local Relationship Preview"
- `local_graph.related_count`: "{count} related cards"
- `local_graph.empty`: "No deterministic relationships found yet..."
- `local_graph.section_title`: "Local relationships"

### 5. 测试更新 (`tests/test_web_product_copy.py`)

- `test_local_graph_views_are_visible_list_fallbacks_without_graph_libraries`: 断言从检查硬编码 "Local Graph Preview" 改为检查 i18n key "local_graph.title"

## 未修改的页面

- **ExportPage**: 安全文案已经准确（浏览器本地下载，不写 Obsidian、不同步外部服务），无需修改
- **SetupPage**: 已有 3 步渐进披露 + fake-default 绿色横幅 + 激活确认对话框 + 折叠高级设置，认知负担已优化
- **GraphPage**: 已有 lab/internal 提示说明未实现的 NodeType
- **SensemakingPage**: 已有 prominent LAB/INTERNAL warning banner

## Gates

| Gate | Command | Exit Code |
|------|---------|-----------|
| git diff --check | `git diff --check` | 0 |
| web build | `npm --prefix web run build` | 0 |
| product copy tests | `python -m pytest tests/test_web_product_copy.py -q --tb=short` | 0 |
| ruff check | `ruff check src/ tests/ docs/` | 0 |

## Remaining Debt

- Browser smoke on all pages (dev server not running during this loop)
- User guide docs still reference outdated state (separate docs workstream)
- web_facade.py architectural debt (separate Architecture Quality Reset workstream)

## Safety

- 未修改 core approval semantics
- 未读取 .env / secrets
- 未修改 real provider activation
- 未扩展 Graph/Sensemaking/Entity/Community
- 未引入 RAG/embedding/vector DB
- 未做架构大重构
- 未调用外部服务
